import React from "react";
interface AddModProps {
  setopenAddMusicianPopup: (open: boolean) => void;
}

const AddMusicianPopup: React.FC<AddModProps> = ({
  setopenAddMusicianPopup,
}) => {
  return <div></div>;
};

export default AddMusicianPopup;
